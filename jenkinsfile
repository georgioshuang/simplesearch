pipeline {

    agent none


  stages {
    stage('Docker Build') {
    	agent any
      steps {
      	sh 'sudo docker build -t georgioshuang/django .'
      }
    }
    stage('Docker test') {
    	agent any
      steps {
      	sh 'uname -a'
      }
    }
  }
}
