pipeline {

    agent { dockerfile true }


  stages {
    stage('Docker Build') {
    	agent any
      steps {
      	sh 'docker build -t georgioshuang/django .'
      }
    }
    stage('Docker test') {
    	agent any
      steps {
      	sh 'uname -a'
      }
  }
}

