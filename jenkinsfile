pipeline {

    agent none


  stages {
    stage('Docker Build') {
    	agent any
      steps {
      	sh 'docker build -t georgioshuang/django .'
      }
    }
    stage('Docker hub') {
    	agent any
      steps {
      	sh 'docker push georgioshuang/django'
      }
    }
  }
}
